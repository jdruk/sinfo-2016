<div class="row">
    <div class="small-12 small-centered columns">
        <div class="row text-center">
<p id="notice"><%= notice %></p>

<h1>CURSOS</h1>

<table table class="text-left">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Tipo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td><%= course.name %></td>
        <td><%= course.type_day %></td>
        <td><%= link_to 'Mostrar', course %></td>
        <td><%= link_to 'Editar', edit_course_path(course) %></td>
        <td><%= link_to 'Excluir', course, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo curso', new_course_path , class: 'button'%>
</div>
</div>
</div>