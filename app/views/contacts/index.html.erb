<div class="row">
    <div class="small-12 small-centered columns">
        <div class="row text-center">
<p id="notice"><%= notice %></p>

<h1>CONTATOS</h1>

<table table class="text-left">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Email</th>
      <th>Obs</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contacts.each do |contact| %>
      <tr>
        <td><%= contact.name %></td>
        <td><%= contact.email %></td>
        <td><%= contact.obs %></td>
        <td><%= link_to 'Mostrar', contact %></td>
        <td><%= link_to 'Editar', edit_contact_path(contact) %></td>
        <td><%= link_to 'Excluir', contact, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo contato', new_contact_path, class: 'button' %>
</div>
</div>
</div>
